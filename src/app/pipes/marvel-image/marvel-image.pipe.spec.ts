import { MarvelImagePipe } from './marvel-image.pipe';

describe('MarvelImagePipe', () => {
  it('create an instance', () => {
    const pipe = new MarvelImagePipe();
    expect(pipe).toBeTruthy();
  });
});
