import { environment } from 'src/environments/environment';

export const marvelAPI = environment.marvel;
export const marvelPublicKey = environment.marvelPublicKey;
export const marvelHash = environment.marvelHash;
